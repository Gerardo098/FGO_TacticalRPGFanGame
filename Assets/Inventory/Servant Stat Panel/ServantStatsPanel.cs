using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// ServantStatsPanel is like the character stats panel, but it shows the 
/// stats and abilities of a selected Servant prior to equipping them, such as 
/// when you hover over their button on the equip panel.
/// </summary>
public class ServantStatsPanel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
